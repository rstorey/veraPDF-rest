server:
  applicationConnectors:
  - type: http 
    port: 8080 
  adminConnectors:
  - type: http
    port: 8081 

logging:

  level: INFO

  loggers:
    org.verapdf.rest.resources.ValidateResource: TRACE

  appenders:
    - type: console
      target: stderr
